# -*- coding: utf-8 -*-
"""appd

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1e2B8ay65-LxXawQA2sLcVXL1lCAGSj7b
"""

import pickle
import numpy as np
from flask import Flask, request, jsonify, render_template

app = Flask(__name__)
model = pickle.load(open('model1.pkl', 'rb'))

@app.route('/')
def home():
    return render_template('home.html')

@app.route('/predict',methods=['POST'])
def predict():
    '''
    For rendering results on HTML GUI
    '''
    int_features = [int(x) for x in request.form.values()]
    final_features = [np.array(int_features)]
    result = model.predict(final_features)

    if(int(result)==1):
        output='Sorry ! Suffering'
    else:
        output='Congrats ! you are Healthy' 
    #return render_template('predict.html', prediction_text='Employee Salary should be $ {}'.format(output))
    return(render_template("result.html", prediction=output))

if __name__ == "__main__":
    app.run(debug=True)